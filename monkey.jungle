project.manifest = manifest.xml

# Source roots
base.sourcePath = source

# Resource roots
base.resourcePath = resources

# Per-device resource overrides — add resources-<device>/ folders when needed
